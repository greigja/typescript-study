import { ProjectInput } from './components/project-input.js';
import { ProjectList } from './components/project-list.js';

// Instantiations build web app via constructors
new ProjectInput();
new ProjectList('active');
new ProjectList('finished');
